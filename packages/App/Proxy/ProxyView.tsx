import { PageProps } from "../deps.ts";

export const ProxyView = (Props: PageProps) => {
  // console.log(Props.data.app.env.PORT);
  // console.log(Props.data.app);
  return <h1>Proxy View</h1>;
};
