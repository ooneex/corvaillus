import { RouteType } from "@ooneex/app";

const routeDefinition: RouteType = {
  name: "user_list",
  path: "/users",
  description: "Route description for User List",
};

export default routeDefinition;
